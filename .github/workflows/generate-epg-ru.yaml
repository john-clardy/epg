name: Generate EPG RU

on:
  workflow_dispatch:
  schedule:
    - cron: "0 0 * * *"

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest
    timeout-minutes: 360

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Check EPG site configuration
        env:
          EPG_SITES_RU: ${{ vars.EPG_SITES_RU }}
        run: |
          if [ -z "$EPG_SITES_RU" ]; then
            echo "::error::EPG_SITES_RU is not configured."
            echo "::error::Add it under Settings > Secrets and variables > Actions > Variables."
            exit 1
          fi

          echo "Configured sites: $EPG_SITES_RU"

      - name: Prepare EPG directory
        run: |
          rm -rf public/epg/ru
          mkdir -p public/epg/ru

      - name: Generate EPG
        env:
          EPG_SITES_RU: ${{ vars.EPG_SITES_RU }}
        run: |
          npm run grab -- \
            --sites="$EPG_SITES_RU" \
            --output="public/epg/ru/{site}.xml" \
            --gzip \
            --days=2 \
            --maxConnections=1

      - name: Verify generated files
        run: |
          echo "Generated files:"

          find public/epg/ru \
            -maxdepth 1 \
            -type f \
            -printf "%f - %s bytes\n" \
            | sort

          if ! find public/epg/ru -maxdepth 1 -type f -name "*.xml*" | grep -q .; then
            echo "::error::No EPG files were generated."
            exit 1
          fi

      - name: Commit EPG files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add public/epg/ru

          if git diff --cached --quiet; then
            echo "No EPG changes to commit."
          else
            git commit -m "Update generated EPG"
            git pull --rebase origin master
            git push origin HEAD:master
          fi

      - name: Upload EPG artifact
        uses: actions/upload-artifact@v4
        with:
          name: epg-${{ github.run_number }}
          path: public/epg/ru/
          if-no-files-found: error
          retention-days: 7
          compression-level: 0
